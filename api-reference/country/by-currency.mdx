---
title: Countries by Currency
description: Find countries that use a specific currency.
---

# Countries by Currency

<Info>**Credits:** 1 per call</Info>

Returns all countries that use the specified currency code.

## Request

```bash
GET /services/country/currency/{currency}
```

<ParamField path="currency" type="string" required>
  ISO 4217 currency code. Example: `USD`, `EUR`, `BHD`.
</ParamField>

<CodeGroup>

```bash cURL
curl -X GET "https://api.edge.bh/api/v1/services/country/currency/BHD" \
  -H "Authorization: Bearer sk_live_your_key_here"
```

```python Python
import requests

response = requests.get(
    "https://api.edge.bh/api/v1/services/country/currency/BHD",
    headers={"Authorization": "Bearer sk_live_your_key_here"}
)
```

</CodeGroup>

## Response

```json
{
  "currency": "BHD",
  "countries": [
    {
      "code": "BH",
      "code3": "BHR",
      "name": "Bahrain",
      "official_name": "Kingdom of Bahrain",
      "native_name": { "ara": { "common": "\u0627\u0644\u0628\u062d\u0631\u064a\u0646", "official": "\u0645\u0645\u0644\u0643\u0629 \u0627\u0644\u0628\u062d\u0631\u064a\u0646" } },
      "flag": "\ud83c\udde7\ud83c\udded",
      "flag_url": "https://flagcdn.com/w320/bh.png",
      "capital": "Manama",
      "region": "Asia",
      "subregion": "Western Asia",
      "population": 1501635,
      "area_km2": 765,
      "currencies": [
        { "code": "BHD", "name": "Bahraini dinar", "symbol": ".د.ب" }
      ],
      "languages": { "ara": "Arabic" },
      "calling_code": "+973",
      "timezones": ["UTC+03:00"],
      "latlng": [26, 50.55],
      "borders": ["SAU"]
    }
  ],
  "total": 1
}
```

### Response fields

<ResponseField name="currency" type="string">
  The currency code that was searched.
</ResponseField>

<ResponseField name="countries" type="array">
  List of countries using this currency. Each country uses the full format (same fields as [Country Lookup](/api-reference/country/lookup)).
</ResponseField>

<ResponseField name="total" type="integer">
  Total number of matching countries.
</ResponseField>
