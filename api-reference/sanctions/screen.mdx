---
title: Screen Entity
description: Screen a name against OFAC, UN, EU, UK OFSI, and 350+ government sanctions lists.
---

# Screen Entity

<Info>**Credits:** 5 per call</Info>

Screen a name against sanctioned entities from OFAC SDN, UN Security Council, EU Consolidated, UK OFSI, and 350+ government watchlists. Returns matching entities with a similarity score.

## Request

```bash
GET /services/sanctions/screen
```

<ParamField query="name" type="string" required>
  The name to screen against sanctions lists. Must be between 2 and 200 characters.
</ParamField>

<ParamField query="min_score" type="number" default="0.7">
  Minimum similarity score (0.6 to 1.0). Lower values return more results but with weaker matches.
</ParamField>

<ParamField query="type" type="string">
  Filter by entity type: `Individual`, `Entity`, `Vessel`, or `Aircraft`.
</ParamField>

<ParamField query="source" type="string">
  Filter by sanctions list: `OFAC`, `UN`, or `UK_OFSI`.
</ParamField>

<CodeGroup>

```bash cURL
curl -X GET "https://api.edge.bh/api/v1/services/sanctions/screen?name=John+Smith&min_score=0.8" \
  -H "Authorization: Bearer sk_live_your_key_here"
```

```python Python
import requests

response = requests.get(
    "https://api.edge.bh/api/v1/services/sanctions/screen",
    headers={"Authorization": "Bearer sk_live_your_key_here"},
    params={"name": "John Smith", "min_score": 0.8}
)
```

</CodeGroup>

## Response

```json
{
  "name": "John Smith",
  "matches": [
    {
      "score": 0.921,
      "name": "SMITH, John David",
      "matched_name": "SMITH, John David",
      "type": "Individual",
      "programs": ["SDGT"],
      "country": "United Kingdom",
      "aliases": ["John D. Smith", "J. Smith"],
      "ids": [{"type": "Passport", "value": "GB123456"}]
    },
    {
      "score": 0.812,
      "name": "SMITH, Jonathan",
      "matched_name": "Jonathan Smith",
      "type": "Individual",
      "programs": ["Security Council"],
      "country": "Unknown",
      "aliases": [],
      "ids": []
    }
  ],
  "count": 2,
  "sources": ["OFAC SDN", "UN Security Council", "EU Consolidated", "UK OFSI", "and 350+ government watchlists"],
  "screened_at": "2026-03-17T12:00:00+00:00"
}
```

<Warning>
Sanctions screening is for compliance purposes. Always verify results manually and consult your compliance team before making decisions based on screening results.
</Warning>
